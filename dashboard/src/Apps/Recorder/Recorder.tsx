import { Box } from "@mui/material";

export default function Recorder() {
  return (
    <Box
      sx={{
        height: "100%",
        display: "flex",
        flexDirection: "column",
        gap: 1,
      }}
    >
      my recorder app
    </Box>
  );
}
