import { Typography } from "@mui/material";

export default function Header() {
  return (
    <Typography
      variant="h1"
      style={{
        textAlign: "center",
        fontSize: "1.5rem",
        padding: "0.5rem 0",
      }}
    >
      Time
    </Typography>
  );
}
